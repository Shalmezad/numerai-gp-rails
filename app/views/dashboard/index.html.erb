<table>
  <tr>
    <th>Deme ID</th>
    <th>Generation</th>
    <th>Running</th>
  </tr>
  <% @demes.each do |deme| %>
    <tr>
      <td><%= link_to deme.id, deme_path(deme) %></td>
      <td><%= deme.generation %></td>
      <td><%= !deme.stop %></td>
    </tr>
  <% end %>
</table>
